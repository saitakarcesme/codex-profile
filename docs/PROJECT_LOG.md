# Project log

## 2026-08-13 — v0.1

- Fetched the current official Codex manual before inspecting local state.
- Found Windows Codex Desktop `26.803.10989.0`; its bundled process runs `codex-cli 0.147.0-alpha.6.6 app-server`. The separately installed npm CLI is `0.144.6`.
- Checked out the exact official open-source tags for both binaries. Both resolve `CODEX_HOME` by canonical path, store file auth at `auth.json`, and derive OS-keyring auth keys from that canonical home.
- Confirmed MCP direct-keyring credentials use service `Codex MCP Credentials` and server name/URL keys, not the ChatGPT identity. File/encrypted MCP stores are under the shared Codex home.
- Inspected the live `$CODEX_HOME` without printing credentials. It contains shared config, 727 active session files, archives, skills, plugins, rules, automations, worktrees, attachments, UI project state, and thread/goals/logs/memory/queue databases.
- Rejected a per-profile full-home design because it would duplicate or require brittle links for all of that shared state.
- Chose one shared home plus a transactional file-auth active slot. Added profile fingerprints, collision refusal, outgoing token-rotation capture, operation lock, switch journal, atomic verified writes, and running-process guard.
- Kept Desktop integration as a companion workflow. Current app-server account APIs expose login, account metadata, and rate limits, but no supported external method hot-loads an arbitrary retained credential into the already-running proprietary Desktop process.
- Implemented a truthful local session handoff through Codex `fork`; remote thread ownership remains account-scoped.
- Fixed Windows command discovery after Node could not execute the PowerShell `codex.ps1` shim directly. The adapter now resolves the native executable without a shell.
- Verified the real active account/plan/rate-limit shape through `account/read` and `account/rateLimits/read`; the test output contained no credential values.
- Added synthetic two-account E2E tests covering install/detect/add/list/use/run, refresh-token rotation, duplicate rejection, unknown-account drift, preserved shared session/config/MCP state, and usage presentation.
- Added a one-action Desktop adapter: scoped process discovery, graceful close with bounded fallback, outgoing refresh-state capture through the existing transaction, and relaunch through the official `codex app <workspace>` mechanism.
- Added a small Windows WinForms Change Profile companion and generated one-click per-profile Desktop shortcuts rather than modifying signed/proprietary Desktop assets.
- Added credential-blind shared-state capture/comparison for config and MCP definitions, skills, plugins, rules, Desktop project roots, repository state, and local-session preservation.
- Tightened real Windows profile storage after ACL inspection: protected inheritance now grants only the current user, SYSTEM, and Administrators. The shared Codex home remains untouched.
- Expanded the automated suite to 32 credential-free tests, including abandoned-login timeout, Desktop process isolation, visible-window readiness, installer-free AppID launch, real-auth-slot active-profile display, close-switch-launch ordering, close-failure recovery, workspace restoration, real Windows `.lnk` generation, shortcut sanitization, preflight failure safety, rollback/relaunch, same-identity credential repair, persisted credential-free auth health, companion ClearType/right-anchor/repair behavior, MCP credential-container preservation, and shared-state integrity.
- Real switching exposed that Windows `codex app` reports success after dispatching the `codex://` URI even when a cold Desktop process never appears. Replaced URI-only cold launch with installed AppID discovery plus `shell:AppsFolder` activation, real-process verification, and URI navigation only after the app is running.
- Real switching also exposed that closing the companion window removed the only visible way back. Converted it to a single-instance notification-area companion whose profile menu survives Desktop termination/relaunch, detects manual-login drift, and can switch either retained account directly.
- A later real cold cycle proved the AppID launcher opens Codex without user intervention. Hardened readiness from “any Desktop process exists” to “main process plus app-server exists,” and changed launch retries into a persistent watchdog so transient Windows activation cannot strand Codex closed.
- Manual logout of Secondary revoked the server-side OAuth session and invalidated its retained refresh token. Added isolated target preflight before Desktop shutdown, same-identity-only reauthorization, rollback on post-close failure, and guaranteed relaunch of the outgoing account. During repair, Personal remains open and usable while the supported browser callback waits; abandoned browser logins now time out and return the profile to an explicit reauthorization-required state without touching the active auth slot.
- Codex refreshed its bundled `.system` skills during a real restart. Updated shared-state verification to permit bundled system-skill refreshes while continuing to hash and protect user-owned skills; plugin cache additions are treated similarly, while removals remain failures.
- Reworked the Windows companion from a stock light dialog into a Codex-like dark selector with a custom title bar, active-account summary, owner-drawn profile cards, plan/auth-health states, usage summary, and a single contextual switch action. The active profile cannot trigger an unnecessary Desktop restart. A credential-free render-only preview path supports visual QA without switching accounts.
- Diagnosed Secondary's persistent `reauth-required` display as stale credential-free health metadata: the successful Google login had updated the protected auth snapshot after the older helper process recorded failure. An isolated app-server preflight verified the expected Secondary identity as a valid Free account and atomically returned its health to `ready`; no credential value was logged.
- Changed normal `desktop use` and every companion/tray profile click to be strictly browser-free. Invalid or revoked target credentials now fail before Desktop closes. Browser authentication occurs only from Add Account, `reauth`, or an explicit `--repair-login` opt-in.
- Replaced the dense selector with the requested minimal surface: only `Choose a profile`, round real avatars, a round Add Account control, and a round refresh control. Real Google avatars discovered during the one-time visible account chooser were saved beside each protected profile snapshot; Codex's current account API/token claims do not expose a reliable picture URL, so future profiles fall back to an initial until an avatar is available.
- Rechecked native integration boundaries. The installed signed Desktop package and documented plugin surfaces provide no supported hook into the built-in bottom-left account menu, so v0.1 retains the update-safe single-instance tray companion rather than patching proprietary assets.
- Investigated a real Secondary switch where the credential changed correctly but Codex did not visibly reopen. The audit had accepted background `ChatGPT.exe + app-server` processes even though the UX requirement is a visible foreground window. Relaunch readiness now requires a non-zero top-level window handle and explicitly focuses that window.
- Correlated all nine `ChatGPT Installer (n).exe` download timestamps with the post-launch `codex app <workspace>` navigation call. On the verified Windows CLI this command opens the ChatGPT download flow. Removed that call from Windows relaunch entirely; a live AppID activation test focused the installed Codex window while installer count and newest-file timestamp stayed unchanged.
- Replaced the WinForms selector with a high-DPI WPF surface using Segoe UI Variable typography, smooth hover scaling, high-quality circular composition, and protected 512×512 Google avatars. Added a 52 px bottom-left Codex-adjacent profile bubble that expands to `Switch profile` on hover without modifying Codex internals.
- Made the selector independent of Codex's process tree and automatically reassert it after every switch. If the old UI process disappears during close/relaunch, the worker starts a new instance; if it survives, the single-instance signal brings it back. Live window enumeration verified both the selector and attached profile bubble while Codex remained open.
- Traced a later Secondary switch failure to the credential-free Desktop audit: the isolated OpenAI usage request returned `401 Unauthorized`, so the preflight correctly left Personal and Desktop untouched. The retained file still existed; the earlier manual Codex `Log out` had revoked its server-side OAuth session.
- Repaired Secondary once through the supported OpenAI/Google browser callback and verified it in an isolated home on the Free plan. Personal remained active and verified on Pro; no credential value, personal email, or callback URL was stored in project logs.
- Found that the apparently broken Add Account button had started both its Node worker and isolated `codex login`; repeated clicks were ignored while the hidden 15-minute wait continued. The companion now exposes that busy state, foregrounds the already-open browser login, prevents duplicate helpers, and reports completion/failure visibly.
- Rebuilt the selector as an opaque per-monitor-DPI-aware WPF window after confirming the transparent layered window disabled ClearType and caused the pixelated appearance. The new white Codex-style layout uses the installed Codex logo, three large centered cards, 512 px avatars, vector check/refresh/plus controls, active/inactive/auth-health pills, native window rounding, and non-rasterizing hover feedback.
- Moved the attached launcher from Codex's bottom-left profile area to the bottom-right and replaced the active-avatar bubble with the installed Codex logo. The main window, notification icon, and overlay now resolve Codex's installed icon resources dynamically instead of presenting a PowerShell icon.
- Replaced the oversized white reference-derived selector with a compact 760 x 430 native Codex-gray surface. Reduced avatars to 92 px, tightened typography and spacing, removed maximize behavior, kept all three cards visible without a large canvas, and retained live WPF controls rather than rasterizing or embedding the reference screenshot.
- Removed PowerShell's ownership of the visible window. Added an open-source C# WinExe host that loads the WPF script in-process, declares a stable `OpenAI.CodexProfile` AppUserModelID, and embeds the installed Codex icon. Live process inspection showed the selector under `CodexProfileHost`, with no PowerShell-owned profile window or console/taskbar button.
- Changed Windows startup to hidden-monitor mode. The Startup shortcut targets the custom host with `--start-hidden`; before interaction it exposes only the 54 x 54 bottom-right Codex overlay. Opening the explicit menu signals the existing instance, while successful account switching merely reasserts the hidden monitor and does not reopen the selector over the restored workspace.

## 2026-08-14 — UI and real context handoff verification

- Removed all profile-card backgrounds and reduced the selector to a 690 x 350 Codex-gray surface with 120 px circular avatars, profile labels, an avatar-ring active indicator, and smooth hover scaling. Future profiles use a hidden-scroll horizontal carousel.
- Made the entire custom title bar draggable except for its actual window buttons, replaced the refresh path with a centered 24-unit vector, and moved the bottom-right Codex overlay 42 px inward so it no longer covers Desktop's vertical scrollbar.
- Replaced the small/noisy taskbar icon with the installed high-resolution `chatgpt-tray-dark.ico`. The GUI-host cache key now includes icon bytes, forcing shortcut regeneration to compile a correctly embedded icon when Codex resources change.
- Rebuilt Desktop and Startup shortcuts and live-tested the new single-instance WinExe host. Startup remains hidden, no PowerShell/terminal window owns the UI, and the selector exposes the expected accessible profile/refresh controls.
- Queried each retained account through isolated app-server homes without changing the Desktop auth slot. Personal resolved to the expected Pro identity and Secondary to the expected Free identity. Their live model-catalog intersection contained `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5`, and `gpt-5.4-mini`.
- Ran a three-stage real-account context handoff in the same disposable repository using `gpt-5.6-terra`: Personal created `increment` and a continuity ledger; Secondary received no prior chat, read the shared files, preserved the token, and added `decrement`; Personal returned without either prior chat, preserved Secondary's work, and added `reset`. Independent validation passed all three tests and confirmed ledger actors `Personal → Secondary → Personal-return` with the same token digest. This proves local project context survives identity changes; it does not claim cross-account ownership of a server conversation.
- Added `SETUP.md` with public GitHub installation, first-account import, one-time second-account login, one-click Windows switching, update, recovery, and security guidance.

### Compatibility concerns

- Keyring-only existing auth cannot be exported through a supported Codex interface; v0.1 asks the user to add that account once.
- The Codex auth JSON and app-server protocols are internal compatibility surfaces even though their current implementations are open source. Keep their parsing behind adapters.
- Live macOS/Linux multi-account validation remains outstanding; portable paths, modes, and process discovery are implemented and unit-testable.
- Exact Desktop window/tab focus restoration remains proprietary client state; v0.1 reliably restores the project root registered for the current repository.

## 2026-08-14 — Tauri desktop UI migration

- Replaced the primary WPF/PowerShell selector with a Tauri 2 native shell and framework-free TypeScript/Vite frontend while leaving the profile/auth/switch transaction engine in Node.
- Added a credential-minimizing Rust bridge. The webview receives only profile UUID, label, active/health state, and bounded avatar data; email, local avatar paths, and authentication payloads are stripped.
- Added a 780 x 470 ambient blue selector with live layered gradients/grain, high-resolution circular avatars, a subtle selected badge, card-free Add Account action, selected-profile hover, custom title bar, reduced-motion behavior, and responsive profile wrapping.
- Split native shell setup into Windows and macOS modules. Added tray, close-to-tray, single-instance focus, release-only hidden autostart, Windows MSI/NSIS packaging, and macOS LaunchAgent/bundle configuration.
- Made CLI `desktop menu` and generated Windows shortcuts prefer the Tauri executable. The old WPF/PowerShell host moved to `legacy/windows` and remains only as a source-run fallback.
- Built and opened the optimized Windows release against the real protected profile registry. Accessibility inspection found Personal, Secondary, Ibrahim Sait, and Add Account; no email or credential path reached the webview.
- Verified one-instance behavior, close-to-tray, CLI reopen, and the `HKCU` hidden-start registration. Produced `Codex Profile_0.1.0_x64_en-US.msi` and `Codex Profile_0.1.0_x64-setup.exe`.
- Added automated UI invariants plus headless Edge screenshots at 100%, 125%, and 150% raster scales; all three retained four centered actions with zero horizontal or vertical overflow.
- The shared macOS source and bundle configuration compile path is in place, but a signed macOS build and live two-real-account switch still require Mac hardware.
- Replaced the blue ambient surface with Codex's neutral gray palette and removed the hand-drawn knot. Both the title bar and launcher now resolve `chatgpt-tray-dark.ico` from the installed Codex package at runtime, so no copied proprietary logo enters the repository.
- Added a 52 px transparent Tauri launcher that follows the public Codex window rectangle, remains absent from the taskbar, hides with Codex, and opens the selector through a native focus fallback plus webview click handler. Live Computer Use verification clicked the real-icon launcher and observed the gray selector load all three retained profiles.
